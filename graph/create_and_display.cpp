#include<bits/stdc++.h>
using namespace std;
class Graph{
private:
    int graph[100][100];
    memeset(int,-1,sizeof(graph)/sizeof(graph[0]));
    int v;
public:
    Graph(int number_of_nodes){
    v=number_of_nodes;
    for(int i=0;i<v;i++){
        for(int j=0;j<v;j++){
        graph[i][j]=0
    }
    }
    }

};
int mian(){
}
